{
  "name": "choreographr.com",
  "version": "0.1.0",
  "private": true,
  "description": "Choreographr website — Zola + Tailwind CSS v4 + daisyUI",
  "scripts": {
    "build:css": "tailwindcss -i styles/main.css -o static/css/main.css --minify",
    "dev:css": "tailwindcss -i styles/main.css -o static/css/main.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "daisyui": "^5.0.0",
    "tailwindcss": "^4.1.0"
  }
}
